import ModeToggle from "@/components/ModeToggle";
import React from "react";
import {
  ClerkProvider,
  SignInButton,
  SignUpButton,
  SignedIn,
  SignedOut,
  UserButton,
} from "@clerk/nextjs";

async function page() {
  
  return (
    <div></div>
  );
}

export default page;
